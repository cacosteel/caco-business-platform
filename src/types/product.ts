export interface product {
  id: string;
  code: string;
  name: string;
  category: string;
  description: string;
  unit: string;
  unit_price: number;
  currency: string;
  is_active: boolean;
  created_at: string;
  updated_at: string;
}