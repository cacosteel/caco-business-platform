export interface inquiry {
  id: string;
  company_id: string;
  contact_id: string;
  inquiry_no: string;
  inquiry_date: string;
  status: string;
  notes: string;
  created_at: string;
  updated_at: string;
}